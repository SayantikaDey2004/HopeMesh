# Render deployment configuration for FastAPI backend
# Docs: https://render.com/docs/deploy-fastapi

# 1. requirements.txt (required by Render)
# Please fill in your actual dependencies if not already present.
fastapi
uvicorn[standard]
pydantic
pydantic-settings
# Add other dependencies as needed

# 2. render.yaml (optional, for advanced config)
# Uncomment and edit if you want to use it.
# services:
#   - type: web
#     name: HopeMesh-backend
#     env: python
#     buildCommand: pip install -r requirements.txt
#     startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
#     plan: free
#     envVars:
#       - key: FRONTEND_URL
#         value: <your-frontend-url>
#       - key: DB_URL
#         value: <your-db-url>
#       # Add all required env vars
